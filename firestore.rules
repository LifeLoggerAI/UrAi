
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow any authenticated user to read and write.
    // We can tighten these rules later.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
