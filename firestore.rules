
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes for local development.
    // IMPORTANT: DO NOT use these rules in production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
