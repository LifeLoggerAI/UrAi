rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow all reads and writes for local development.
      // In a production environment, these rules should be
      // tightened to enforce proper user-based security.
      allow read, write: if true;
    }
  }
}
