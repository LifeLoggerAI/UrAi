
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This is an insecure rule for development purposes only.
    // It allows anyone to read and write to the database.
    // TODO: Secure this before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
