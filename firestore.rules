rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for any authenticated user during development.
    // This is insecure for a production environment but necessary for local testing.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
