
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For development, allow reads and writes if the user is authenticated.
    // In production, you should secure your data with more specific rules.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
