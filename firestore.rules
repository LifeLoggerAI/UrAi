
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Default rule: Allow read/write access to any logged-in user.
    // This is a permissive rule for development and will fix the save error.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
