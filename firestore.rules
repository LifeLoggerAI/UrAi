rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // This is an insecure rule for development purposes only.
    // It allows all reads and writes to the entire database.
    // TODO: Replace with secure rules before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
