
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to all documents for local development.
    // This is not secure for production but is necessary for the emulator.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
