{
  "name": "urai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint --ext .ts,.tsx src || true",
    "typecheck": "tsc --noEmit",
    "ci": "npm ci && npm run typecheck && npm run lint && npm run build",
    "health": "bash ./scripts/baseline_health_check.sh"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "typescript": "^5.0.0"
  }
}