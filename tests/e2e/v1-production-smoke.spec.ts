import { test } from "@playwright/test";

test.describe.skip("archived production smoke assertions", () => {
  test("current production smoke coverage moved to release-smoke.spec.ts", async () => {});
});
