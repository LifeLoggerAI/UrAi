# Genesis Publish Attempt

Timestamp: 2026-06-30T02:00:00-05:00
Repo: LifeLoggerAI/UrAi
Branch: main

## Purpose

This safe proof-only commit records a production publish attempt for the existing GitHub Actions deployment path.

## Existing deployment path

The repo has `.github/workflows/deploy.yml` configured for pushes to `main` and manual dispatch.

Expected high-level steps:

1. checkout
2. set up Node 20
3. install dependencies
4. run release gates
5. build
6. deploy Firebase Hosting live with `FIREBASE_SERVICE_ACCOUNT_URAI`
7. verify Firebase-hosted routes
8. verify custom-domain linked routes with `npm run smoke:linked-routes:live`
9. verify XR/AR live contract

## Live state before this proof commit

- `https://urai.app/ground`: reachable and launch-safe.
- `https://urai.app/status`: reachable but stale; still shows old monitoring and service-health copy.
- `https://urai.app/system`: not verified by web fetch; cache miss.

## Success criteria

This repo can only be treated as published from this attempt if receipts exist for:

- successful build/test/release gates
- Firebase Hosting deploy success
- release ID
- deployed source SHA
- live `https://urai.app/status` showing preview-safe copy
- live `https://urai.app/system` showing registry truth markers
- `npm run smoke:linked-routes:live` passing

## Current status

Source-side publish attempt recorded. Do not mark production-ready without the receipts above.
