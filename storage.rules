
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Narration audio (signed URL OR public read — choose one)
    match /narration/{allPaths=**} {
      allow read: if true;
      allow write: if false;
    }

    // Default closed
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
