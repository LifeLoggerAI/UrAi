# Next Wiring Tasks

Updated: 2026-06-25

## Priority 0

1. Lock `UrAi` canonical target and cross-repo env contract
2. Keep `UrAi-Dev` and `UrAiProd` out of runtime truth
3. Adopt `urai-privacy` release gating into the Genesis spine repos
4. Make `urai-admin` the only release-control plane
5. Keep `urai-jobs` as the only approved async execution backend
6. Consume `urai-content` as a source package, not a required standalone launch site

## Priority 1

1. Prove `urai-staging` as the only integration proving ground
2. Prove `asset-factory` against the verified Firebase base URL
3. Prove `urai-spatial` Tier-1 staging behavior
4. Prove `urai-analytics` only after privacy and durable storage evidence

## Priority 2

1. Fix domain drift for intended live URAI-operated domains
2. Keep `urai-communications` disconnected until privacy/provider proof is complete
3. Keep `urai-storytime` disconnected until safety/legal/provider proof is complete
4. Complete `urai-foundation` DNS cutover
