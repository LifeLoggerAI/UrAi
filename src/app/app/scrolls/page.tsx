import SystemRoutePage from "@/components/SystemRoutePage";

export default function ScrollsPage() {
  return (
    <SystemRoutePage
      title="Scrolls"
      description="Weekly recaps, recovery blooms, story scripts, and shareable reflection scrolls are collected here."
      status="wired"
    />
  );
}
