import { HomeScene } from "@/components/urai/home/HomeScene";

export const metadata = {
  title: "URAI App Shell",
  description: "Authenticated URAI shell entry.",
};

export default function AppIndexPage() {
  return <HomeScene />;
}
