import SystemRoutePage from "@/components/SystemRoutePage";

export default function RitualsPage() {
  return (
    <SystemRoutePage
      title="Rituals"
      description="Ritual suggestions, completion state, recovery blooms, and weekly scroll handoffs are surfaced here."
      status="wired"
    />
  );
}
