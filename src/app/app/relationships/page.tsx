import SystemRoutePage from "@/components/SystemRoutePage";

export default function RelationshipsPage() {
  return (
    <SystemRoutePage
      title="Relationship Intelligence"
      description="Relationships, socialGraph, attachment context, trust patterns, and relationship rituals are grouped behind consent-gated processing."
      status="guarded"
    />
  );
}
