import SystemRoutePage from "@/components/SystemRoutePage";

export default function StoryPage() {
  return (
    <SystemRoutePage
      title="Story Mode"
      description="Gated story preview surface. Scripts, narrator voiceover, captions, CapCut/SRT handoff, and export jobs remain sample or roadmap-only until authenticated jobs, provider safety, storage ownership, and smoke evidence are proven."
      status="guarded"
    />
  );
}
