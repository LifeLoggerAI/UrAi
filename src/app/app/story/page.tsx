import SystemRoutePage from "@/components/SystemRoutePage";

export default function StoryPage() {
  return (
    <SystemRoutePage
      title="Story Mode"
      description="Story scripts, narrator voiceover, captions, CapCut/SRT handoff, and export jobs converge in the story assembly surface."
      status="guarded"
    />
  );
}
