import SystemRoutePage from "@/components/SystemRoutePage";

export default function ExportsPage() {
  return (
    <SystemRoutePage
      title="Export Hub"
      description="PDF, PNG, story, SRT, and video export jobs are tracked here with private-by-default storage and cleanup expectations."
      status="guarded"
    />
  );
}
