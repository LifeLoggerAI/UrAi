'use client';
import { SettingsForm } from '@/components/settings-form';

export default function Settings() {
  return (
    <main className="mx-auto max-w-2xl p-4 md:p-8 text-foreground">
      <SettingsForm />
    </main>
  );
}
