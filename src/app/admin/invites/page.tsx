import SystemRoutePage from "@/components/SystemRoutePage";

export default function AdminInvitesPage() {
  return (
    <SystemRoutePage
      title="URAI Admin Invites"
      description="Admin Invites is the guarded operator shell for reviewing invite codes, early access cohorts, and onboarding status."
      status="guarded"
    />
  );
}
