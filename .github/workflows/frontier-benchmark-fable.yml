name: URAI Fable Frontier Benchmark

on:
  push:
    branches: [agent/frontier-benchmark-harness]
    paths:
      - ".github/workflows/frontier-benchmark-fable.yml"
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    env:
      ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Generate frozen synthetic suite
        run: node scripts/bench/generate-long-horizon-v2.mjs bench/suites/urai-long-horizon-v2.jsonl

      - name: Provider readiness
        run: node scripts/bench/doctor.mjs

      - name: Expanded Fable paired A/B
        run: |
          echo "Starting frozen 60-task Fable paired benchmark"
          node scripts/bench/run-frontier-bench.mjs \
            --suite bench/suites/urai-long-horizon-v2.jsonl \
            --providers="fable,fable+urai" \
            --repeats=1 \
            --max-output-tokens=1200 \
            --run-id ci-fable-v2 \
            --fail-on-errors

      - name: Upload benchmark evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: frontier-benchmark-evidence-v2-fable
          path: |
            bench/results/
            bench/suites/urai-long-horizon-v2.jsonl
          if-no-files-found: error
