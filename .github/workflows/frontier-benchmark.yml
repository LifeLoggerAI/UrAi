name: URAI Frontier Benchmark

on:
  workflow_dispatch:

jobs:
  benchmark:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Upgrade in progress
        run: echo "Benchmark workflow temporarily paused while the v2 suite is installed."
