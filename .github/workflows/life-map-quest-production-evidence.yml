name: Legacy Life Map Production Evidence — Quarantined

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deny:
    runs-on: ubuntu-24.04
    steps:
      - name: Deny legacy Life Map production evidence
        run: |
          echo "This legacy repository cannot certify the deployed Life Map." >&2
          echo "Use the canonical urai-spatial protected release evidence." >&2
          exit 1
